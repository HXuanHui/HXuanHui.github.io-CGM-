#postgreSQL 

備份檔預設會放在 **/var/lib/pgbackrest** 這個目錄中，也有可能在建置時修改放在別處。

**設置來源：**

1. **建立目錄階段：** 文件中提到「建立備份儲存庫 `/var/lib/pgbackrest` 資料夾會自動被建立」。

2. **設定檔階段：** 在 `pgbackrest.conf` 中，參數 `repo1-path` 被設定為 `/var/lib/pgbackrest`,,。

3. **驗證：** 您可以透過檢查該目錄下的內容（會有 `backup`, `archive` 等子目錄）來確認備份實體檔案的位置。